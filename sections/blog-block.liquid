{% for block in section.blocks %}
  <div class="page-width max-w-none py-8" {{ block.shopify_attributes }}>
      <p>{{ block.settings.blog_block_content }}</p>
  </div>
{% endfor %}

{% schema %}
{
    "name": "Blog Block",
    "max_blocks": 4,
        "blocks": [
          {
            "type": "Blog",
            "name": "Blog Block",
            "settings": [
              {
                  "type": "textarea",
                  "id": "blog_block_content",
                  "label": "label"
              }
            ]
          }
        ]
}
{% endschema %}